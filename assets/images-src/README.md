# Image Source Directory

Place unoptimized source images for the landing page in this folder.

- Supported formats: `.png`, `.jpg`, `.jpeg`
- Subdirectories mirror `public/images/` to keep output organized.
- Run `pnpm generate:images` to produce optimized AVIF/WebP/JPEG variants and blur placeholders in `public/images/`.
